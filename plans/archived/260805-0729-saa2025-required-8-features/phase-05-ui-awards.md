# Phase 05 — UI · Hệ thống giải / Awards (STT 8, Track A)

**Screen:** Hệ thống giải (zFYDgyj_pD) · **Status:** ✅ built.
**Goal:** award-system landing — hero + left-nav (6 categories) + award cards (icon/title/qty/prize) + Kudos promo footer. Mostly static; smooth-scroll + active-state menu.
**Files:** `src/features/awards/components/**`, `src/features/awards/award-config.ts`, `src/app/awards/page.tsx`.
**Out of scope:** no CRUD; 6 award defs are static config.
**Integration contract:** auth-gated; award defs from static config; hashtag anchor from Homepage card click.
