/**
 * Font setup for the Kudos feature.
 * Re-exports from auth fonts to avoid duplicate font registrations
 * (Next.js deduplicates font instances by config hash).
 */
export { montserrat } from '../auth/fonts'
